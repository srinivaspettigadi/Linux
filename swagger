swagger:
  image: swaggerapi/swagger-ui
  ports:
    - "8080:8080"
  environment:
    - SWAGGER_JSON=/path/to/your/swagger.json # Mount your Swagger JSON file
  networks:
    - appnet
  deploy:
    replicas: 1 # You usually need only one Swagger UI instance

