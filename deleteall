login to ECS instance:

docker ps 
docker stop <container name>
docker rmi $(docker images -a -q)
docker system prune -a



Scale down the instances to zero in autoscaling group 
Delete the ECS cluster
